# Context
0. 모든 데이터의 출처는 Daily Note와 Weekly Note이다.
1. Weekly Note의 Template은 다음과 같다.
```markdown
### Goal

### Tracker
```
# Tasks
- [x] recent15와 weekly view에서 현재 날짜에 해당하는 weekly goal, monthly goal, yearly goal을 전부 표시하도록 수정하라.
- [x] 기존 dashboard의 table과 plot에 표시하는 날짜의 범위가 view의 주 기능이었지만, 해당 기능을 predefined 필터 항목으로 옮겨라. 대신 기존 weekly/monthly/yearly view는 이제 전혀 다른 GUI layout을 활용한다. 각 layout은 다음과 같다.
  - Weekly view: 오늘이 속한 week에 해당하는 범위의 daily Note의 데이터를 가져와 table과 plot으로 표시한다. 기존과 크게 다르지 않다.
  - Monthly view: 오늘이 속한 month에 해당하는 범위의 daily Note의 데이터를 가져와 table과 plot으로 표시한다. 추가로, 이번 달에 해당하는 weekly goal 4개와 Monthly goal 역시 상단에 표시한다. 물론, yearly goal도 마찬가지다.
  - Yearly view: 오늘이 속한 year에 해당하는 범위의 daily Note의 데이터를 가져와 table과 plot으로 표시한다. 추가로, 올해 weekly goal 전부 와 monthly goal을 상단에 표시한다.
  - layout 지시가 모호한 경우, toss 급의 세계 탑급 프론트엔드 완성도로 view의 layout을 설계하고 구현하기 전 승인을 받기 위해 예시를 제공하라.



# 디버깅 리스트
- [x] Weekly goal을 dashboard에서 추가할 때 "쇼핑하기"를 입력하면 goal이 "쇼핑하기"와 "기"가 입력되는 의도치 않은 동작이 발생한다. 한국어를 입력할 때만 이런 현상이 발생하는 것으로 보인다. 이 문제를 해결하고, 추가로 입력한 문자열만 정확하게 weekly note의 "### Goal" 과 그 다음 ### 헤더 사이의 영역에 기록되도록 하라. 
- [x] Weekly goal의 양방향 업데이트 메커니즘에 치명적이 오류가 의심된다. Weekly Note에서 직접 goal을 추가하거나, dashboard 상에서 goal을 추가하고 나서, view를 monthly view나 yearly view로 전환 한 후에 다시 돌아오면 입력해놓은 것들이 사라져있거나, 이전에 기록되어있던 goal들이 기록되어 있도록 바뀌어 있다. 양방향 업데이트를 보장하도록, dashboard에서 기록하든, weekly note에서 직접 기록하든, 기록된 데이터의 유실이 발생하지 않는 것을 보장하도록 현재 양방향 업데이트 메커니즘을 점검하여 원인을 찾고 해결하라.
- [x] 모바일 버전에서는 각 goal들의 배치를 multi-column이 아니라 single-column으로 수정하라. PC에서는 그대로 multi-column이어야 한다.
- [x] Translucent 디자인 철학으로 모듈들의 디자인을 수정하라.
  - [x] 아예 투명해야 한다. 테두리나, 입력하는 모듈 정도만 약간의 blur가 있는 정도다. '평가'컬럼이나 '할 일' 컬럼의 입력하는 창을 참고하되, 가능하다면 아예 투명하게 해도 좋다.
- [x] 세로 스크롤 시에 goal부분은 상단에 고정이지만 plot과 table은 한 몸처럼 스크롤된다. 단, 가로 스크롤 시에는 plot은 가만히 있고 table만 스크롤된다.
- [x] 앱을 열어보면 모듈이 전혀 투명하지 않다. 모듈의 배경이 투명해서 Obsidian에서 설정한 배경이미지가 보이도록 수정하라.
- [x] goal을 dashboard에서 입력할 때 마치 Table에서 "할 일" 컬럼에 입력하듯이, 아무 내용이 없을 때만 '+' 표시가 뜨고, 하나를 입력하고 엔터를 누르는 순간 마치 inline에서 입력하듯이 아래에 다음 checkbox가 생성되면서 line breaking이 되어야 한다.
  - [x] 목표 항목이 아예 없을 때를 제외하고는 '+ 추가' 버튼이 없어야 한다. 모든 입력은 엔터를 쳤을 때 inline line breaking에 의해서 이뤄져야 한다. "할 일" 컬럼에서 새로운 데이터를 기록할 떄의 cell 컴포넌트를 참고하라!
  - [x] 현재 동작을 테스트한 결과 "+ 추가" 버튼을 눌러 활성화 된 상태에서만 엔터를 쳐서 line breaking이 가능하다. 그러니까. "+ 추가" 버튼을 없애도 항상 가능하도록 기능을 수정하라.
- [x] 목표의 순서는 어떤 view던지 간에 반드시 weekly goal, monthly goal, yearly goal 순서여야 한다.
  - [x] '이번 주 목표', '이번 달 목표', '연간 목표'의 (토글 기능이 있는) 제목 컴포넌트의 공간 낭비가 크다. "주간 목표", "월간 목표", "올해 목표"로 이름을 바꾸어라.
- [x] 최근 7일, 최근 15일, 이번 주, 이번 달, 올해, 전체에 해당하는 필터 버튼의 위치를 table과 plot 사이 "+ 필터" 버튼 오른쪽으로 이동시켜라.
- [x] 모바일 버전에서 '할 일', '평가'의 경우에 입력하려고 입력모듈을 터치해 focus시키면 키보드가 튀어나왔다가 화면이 뭔가 깜빡거리면서 다시 키보드가 사라져 무언가를 입력할 수 없다. 원인을 찾고 해결하라.
- [x] 모바일 버전에서 '주간 목표', '월간 목표', '올해 목표'를 입력하려고 하면 키보드가 올라오면서 화면의 아래부터 90퍼정도 영역에 있는 렌더링이 모두 사라져버리는(투명해져 없어진 것같은) 현상이 존재한다. 원인을 찾고 해결하라.
- [x] 모바일에서는 화면이 가로로 짧기 때문에 '주간 목표', '월간 목표', '올해 목표'를 3-column으로 하지 말고 단일 컬럼으로 렌더링 되도록 하라. PC에서는 그대로! 3-column으로 유지해야 한다. 불가능하다면 이유를 제시하라.
  - [x] 모바일에서는 토글이 접혀있더라도 각 '주간 목표', '월간 목표', '올해 목표' 별로 글씨 바로 오른쪽에 현재 날짜와 매칭되는 목표(만약 해당 날짜와 매칭되는 목표가 여러개라면 그중 가장 첫 번째 목표)를 디스플레이 하도록 하라! 당연히 checkbox 양방향 편집 및 업데이트도 정상 동작해야 한다.
- [ ] 안드로이드 모바일 환경에서 데이터 입력을 위해 키보드가 아래에서 올라오는 모든 경우 다음과 같은 증상이 발생한다.
    - [ ] 스크롤과 무관하게 고정된 상단 헤더부분을 제외한 plot과 table 영역이 키보드 height 정도만큼 밀려 올라가면서 고정된 상단 헤더부분의 대부분을 가려버린다.
    - [ ] 밀려 올라간 plot과 table 영역이 온전히 다 보이는 채로 밀려올라가는 것이 아니라, 투명한 가림막에 가려진 채로 올라간다. 
